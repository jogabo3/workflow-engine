workflows:
  - name: failing_demo
    source:
      type: local
      location: examples/failing_demo

    adapter: command

    steps:
      - name: fail_step
        command: python run.py

    execution:
      continue_on_failure: false
      retries: 0
      timeout_seconds: 30
      resume_from_checkpoint: false
